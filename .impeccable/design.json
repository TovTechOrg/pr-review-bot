{
  "schemaVersion": 2,
  "generatedAt": "2026-09-06T00:00:00Z",
  "title": "Design System: Autonomous Code Review Engine — Dashboard",
  "extensions": {
    "colorMeta": {
      "ink": { "role": "neutral", "displayName": "Ink", "canonical": "#16150f", "tonalRamp": ["#16150f", "#26241c", "#3a382c", "#524f40", "#6c6857", "#89846f", "#a8a38b", "#c9c4a9"] },
      "paper": { "role": "neutral", "displayName": "Paper", "canonical": "#f1efe6", "tonalRamp": ["#5c5a4d", "#79766650", "#918d7a", "#a9a591", "#c1beac", "#d8d6c8", "#e9e7dd", "#f1efe6"] },
      "surface": { "role": "neutral", "displayName": "Window Surface", "canonical": "#faf9f2", "tonalRamp": ["#5c5b52", "#78766b", "#918f83", "#a9a89e", "#c1c0b7", "#d8d7d0", "#ecebe6", "#faf9f2"] },
      "muted": { "role": "neutral", "displayName": "Muted Ink", "canonical": "#5c5a4d", "tonalRamp": ["#242318", "#3a3826", "#524f37", "#6c684a", "#89855e", "#a6a179", "#c4c0a1", "#e2e0cd"] },
      "signal": { "role": "primary", "displayName": "Signal Amber", "canonical": "#a3550a", "tonalRamp": ["#2b1502", "#4d2705", "#733a09", "#a3550a", "#c97a2e", "#dd9c5e", "#ecbf93", "#f7e0cb"] }
    },
    "typographyMeta": {
      "display": { "displayName": "Display (Pixel)", "purpose": "Titles only: h1, .win-title, .env-section h2, h1.login-title. Never body/buttons/nav." },
      "body": { "displayName": "Body", "purpose": "Default readable text: stat values, table cells, inputs, review rows." },
      "label": { "displayName": "Label", "purpose": "Muted secondary text: kv-label, field hints/labels, table headers, timestamps." }
    },
    "shadows": [],
    "motion": [
      { "name": "signal-pulse", "value": "scale(1) -> scale(1.8) -> scale(1) over 0.6s ease-out", "purpose": "One-shot pulse on a .win-live dot the instant it goes live (queue running / specialist failed)." },
      { "name": "ease-standard", "value": "0.15s-0.3s ease", "purpose": "Thread/dot color-and-opacity transitions, tooltip fade, chevron rotation." }
    ],
    "breakpoints": [
      { "name": "sm", "value": "640px" }
    ]
  },
  "components": [
    {
      "name": "Window",
      "kind": "card",
      "refersTo": "window",
      "description": "The core primitive replacing cards system-wide: a titled, hairline-bordered, flat panel with an inverted title bar.",
      "html": "<div class=\"ds-win\"><div class=\"ds-win-title\"><span>Queue</span><span class=\"ds-win-live\"></span></div><div class=\"ds-win-body\"><div class=\"ds-kv-row\"><span class=\"ds-kv-label\">Total reviews</span><span class=\"ds-kv-value\">42</span></div></div></div>",
      "css": ".ds-win { background: #faf9f2; border: 1px solid #16150f; border-radius: 0; box-shadow: none; } .ds-win-title { display: flex; align-items: center; justify-content: space-between; gap: 0.5rem; background: #16150f; color: #f1efe6; font-family: \"Press Start 2P\", -apple-system, sans-serif; font-size: 0.6rem; line-height: 1.6; letter-spacing: 0.03em; text-transform: uppercase; padding: 0.45rem 0.65rem; } .ds-win-live { width: 0.4rem; height: 0.4rem; background: #f1efe6; opacity: 0.5; transition: background-color 0.3s ease, opacity 0.3s ease; } .ds-win-live.is-live { background: #a3550a; opacity: 1; } .ds-win-body { padding: 0.85rem 1rem; } .ds-kv-row { display: flex; justify-content: space-between; align-items: baseline; gap: 0.5rem; padding: 0.35rem 0; border-bottom: 1px dashed #dad6c4; } .ds-kv-label { color: #5c5a4d; font-size: 0.85rem; } .ds-kv-value { font-weight: 600; }"
    },
    {
      "name": "Traceable Thread",
      "kind": "custom",
      "refersTo": null,
      "description": "Dashed connector joining two windows in a real pipeline relationship; switches from muted to solid signal-amber only while the tracked state is actually live.",
      "html": "<div class=\"ds-win\"><div class=\"ds-win-title\"><span>Queue</span></div><div class=\"ds-win-body\">running</div></div><div class=\"ds-thread is-live\"></div><div class=\"ds-win\" style=\"margin-top:1.5rem\"><div class=\"ds-win-title\"><span>Security</span></div></div>",
      "css": ".ds-thread { height: 1.5rem; margin-block: -1.5rem 0; margin-inline-start: 1.5rem; border-inline-start: 2px dashed #5c5a4d; width: 0; transition: border-color 0.3s ease; } .ds-thread.is-live { border-inline-start-color: #a3550a; } .ds-win { background: #faf9f2; border: 1px solid #16150f; box-shadow: none; } .ds-win-title { background: #16150f; color: #f1efe6; font-family: \"Press Start 2P\", -apple-system, sans-serif; font-size: 0.6rem; letter-spacing: 0.03em; text-transform: uppercase; padding: 0.45rem 0.65rem; } .ds-win-body { padding: 0.85rem 1rem; }"
    },
    {
      "name": "Control Button",
      "kind": "button",
      "refersTo": "button-control",
      "description": "The default toolbar/nav button: hairline border, pressing-inverts state idiom instead of a colored hover ring.",
      "html": "<button class=\"ds-btn-control\">Status</button>",
      "css": ".ds-btn-control { display: inline-flex; align-items: center; gap: 0.4rem; background: #faf9f2; color: #16150f; border: 1px solid #16150f; border-radius: 0; padding: 0.4rem 0.9rem; font-size: 0.9rem; cursor: pointer; } .ds-btn-control:hover { border-color: #16150f; background: #f1efe6; } .ds-btn-control:focus-visible { outline: 2px solid #a3550a; outline-offset: 1px; } .ds-btn-control.active { background: #16150f; color: #f1efe6; }"
    },
    {
      "name": "Submit Button",
      "kind": "button",
      "refersTo": "button-submit",
      "description": "The one filled-at-rest control (login submit) — inverted ink fill, never amber since there's nothing to alert on there.",
      "html": "<button class=\"ds-btn-submit\">Sign in</button>",
      "css": ".ds-btn-submit { width: 100%; padding: 0.6rem; border: 1px solid #16150f; border-radius: 0; background: #16150f; color: #f1efe6; font-size: 0.95rem; cursor: pointer; } .ds-btn-submit:hover { background: #5c5a4d; border-color: #5c5a4d; } .ds-btn-submit:focus-visible { outline: 2px solid #a3550a; outline-offset: 1px; }"
    },
    {
      "name": "Input Field",
      "kind": "input",
      "refersTo": "input",
      "description": "Text/password input: flat surface fill, hairline border, no radius, amber focus outline.",
      "html": "<input type=\"text\" class=\"ds-input\" placeholder=\"KEY_NAME\">",
      "css": ".ds-input { font: inherit; color: #16150f; background: #faf9f2; border: 1px solid #16150f; border-radius: 0; padding: 0.4rem 0.6rem; } .ds-input:focus-visible { outline: 2px solid #a3550a; outline-offset: 1px; } .ds-input[readonly] { opacity: 0.7; }"
    },
    {
      "name": "Chip",
      "kind": "chip",
      "refersTo": null,
      "description": "Inline tag for queue-status counts / provider-backoff entries: hairline border, soft-dither fill, no radius.",
      "html": "<span class=\"ds-chip\">Running: 2</span>",
      "css": ".ds-chip { display: inline-block; background: #faf9f2; background-image: radial-gradient(rgba(22,21,15,0.22) 1px, transparent 1px); background-size: 6px 6px; border: 1px solid #16150f; border-radius: 0; padding: 0.15rem 0.45rem; font-size: 0.8rem; white-space: nowrap; }"
    },
    {
      "name": "Navigation Item",
      "kind": "nav",
      "refersTo": null,
      "description": "Pill-tab nav button with an authored stroke-SVG icon; active state fully inverts.",
      "html": "<button class=\"ds-btn-control ds-nav-item active\"><svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><polyline points=\"22 12 18 12 15 21 9 3 6 12 2 12\"/></svg><span>Status</span></button>",
      "css": ".ds-nav-item svg { width: 1.05rem; height: 1.05rem; display: block; flex: none; } .ds-nav-item.active { background: #16150f; color: #f1efe6; border-color: #16150f; }"
    }
  ],
  "narrative": {
    "northStar": "The One-Bit Windowed Console",
    "overview": "This is a small desktop, not a scrolling report. Every persistent surface — Queue, each specialist, Activity.log, each Environment section — is an independent titled window with a solid inverted title bar, joined to the desktop's dithered ground rather than floating on a plain page. The palette is near-monochrome ink-on-paper with exactly one accent reserved for alert/active state; the type voice pairs a self-hosted one-bit pixel display face on titles only against a plain system sans everywhere else a solo operator actually has to read fast. Nothing here is decorative: the dither is a real background texture (not an unused token), the dashed \"traceable thread\" connector lights up only when a real pipeline relationship is active, and the single amber signal never appears except to mean \"alert\" or \"running.\"\n\nThe system was built to replace a flat neutral-gray/blue-accent \"Instrument Panel\" world (the previous DESIGN.md) — that world is gone; nothing here should be read as a refinement of it. A hard offset drop-shadow was tried for dialog elevation during the build and explicitly rejected at finish review: it broke the one-bit material's own commitment to no soft/blurred or ambient depth, so box-shadow: none was restored everywhere, including on dialogs and popups.",
    "keyCharacteristics": [
      "Independent window chrome (inverted title bar + hairline border) replaces cards everywhere.",
      "Exactly one accent color (amber/signal), used only for alert or active state, never decoratively.",
      "Ordered dither stands in for a secondary/recessed surface, used as real background texture (desktop ground, chip/grid fills), not a swatch that goes unused.",
      "A self-hosted pixel display face is reserved for titles/headings only; body and controls stay in the system sans for scanability.",
      "Fully flat: no shadow anywhere, hairline borders carry all separation and depth.",
      "Plain key : value rows replace stat-tile grids as the default way to present a fact."
    ],
    "rules": [
      { "name": "The One Warm Signal Rule", "body": "The amber accent appears only in response to a real state change (something is running, something failed, something needs attention). It is never applied to a default, idle, or purely decorative element — an idle dot, an idle thread, and an OK status all render in ink or muted, never amber.", "section": "colors" },
      { "name": "The Full Inversion Rule", "body": "Dark mode is not a re-tint; --ink and --paper swap roles wholesale (and every other token derives from that swap), so window title bars, which are always \"ink-filled, paper-text,\" read correctly as inverted chrome in both themes without special-casing.", "section": "colors" },
      { "name": "The Titles-Only Pixel Rule", "body": "The self-hosted pixel display face is applied only to h1, .win-title, .env-section h2, and h1.login-title — never to body text, buttons, nav labels, or table content. This is a deliberate Operate-mode scanability choice, not an oversight.", "section": "typography" },
      { "name": "The Graceful Hebrew Degradation Rule", "body": "Press Start 2P has no Hebrew glyphs. Its font stack's fallback chain lets Hebrew text degrade per-character to the system sans automatically under dir=\"rtl\" — this is intentional and correct, not a missing glyph to chase down.", "section": "typography" },
      { "name": "The No-Shadow, No-Exceptions Rule", "body": "Every elevated surface — window, popup, dialog, tooltip, modal backdrop — uses box-shadow: none and a hairline border instead. A hard offset shadow was tried and rejected at finish review; don't reintroduce one for a new surface.", "section": "elevation" }
    ],
    "dos": [
      "Do give every persistent panel real window chrome (.win/.win-title or .env-section/h2) — an inverted title bar and a hairline border — never a plain card.",
      "Do reserve the amber signal color exclusively for alert/active state (a live dot, a running thread, a failed status, a critical finding, a focus ring). If a new element needs color and it isn't alerting or active, use ink or muted, never amber.",
      "Do apply the self-hosted pixel display face only to h1/.win-title/.env-section h2/h1.login-title. Every other text — buttons, nav, body, table cells — stays in the system sans.",
      "Do keep every corner square (border-radius: 0) and every shadow absent (box-shadow: none) on every new component; hairline borders are this system's only separation device.",
      "Do author new icons as inline SVG matching the existing stroke convention (viewBox 0 0 24 24, stroke=\"currentColor\", stroke-width=\"2\", round caps/joins) — never an icon font or a raster image.",
      "Do use .kv-row (plain label/value pairs) as the default way to present any fact or stat, including on new panels.",
      "Do use logical CSS properties (inset-inline-end, border-inline-start, padding-inline-end) for anything positioned relative to text direction, so it flips correctly under Hebrew RTL without a separate RTL stylesheet."
    ],
    "donts": [
      "Don't use a hero-metric/stat-tile-grid pattern (a big number alone in its own bordered box) for stats or summaries — this build replaced that exact pattern with .kv-row specifically because nested/stacked cards for individual numbers read as unearned decoration on this surface; treat the stat-tile grid as a retired anti-pattern, not an available alternative to .kv-row.",
      "Don't add a box-shadow to any element, including a \"just this once\" dialog/popup elevation need — a hard offset shadow was tried for exactly that during this build's finish review and rejected as contradicting the one-bit material's own no-shadow commitment. This is a hard invariant of this world, not a case-by-case judgment call.",
      "Don't use emoji as icons anywhere in this system — every icon is an authored inline SVG in the stroke convention described above.",
      "Don't load the display font from a third-party CDN (e.g. Google Fonts) — it is self-hosted from this app's own /static/fonts mount specifically so an unreachable third-party request can't silently revert the display voice on a health-status page.",
      "Don't invent a persistent sidebar navigation for today's two-panel scope. A sidebar is a confirmed acceptable future scaling path once more panels exist, but the current build's pill-tab .side-nav row is the deliberate choice for two panels — don't promote the future option into a present rule."
    ]
  }
}
