[extend]
useDefault = true

# Kept byte-identical to the sibling onboarding-wizard repo's copy of this
# file (same convention as .claude/hooks/ — see CLAUDE.md).
[allowlist]
description = "Synthetic fixture value used throughout tests/test_*_hook*.py to exercise the .env redaction hooks (SOME_KEY=abcdefgh12345678 / GITHUB_WEBHOOK_SECRET=abcdefgh12345678) — not a real credential. See ISSUES.md's 2026-09-05 Stripe-shaped-placeholder entry for the class of false positive this guards against recurring. Second entry: the demo image's baked Fernet key (Dockerfile.demo) — a structurally real Fernet key (main.py constructs Fernet(key) at boot and refuses a placeholder) that encrypts nothing sensitive, since demo/session_store.py drops encryption entirely. It was always destined to be committed in that exact file."
regexes = ['''abcdefgh12345678''', '''xZj-oNDwS8lUbvwHUFUjlfRzd2rKasDc7N2ov7DDtBw=''']
